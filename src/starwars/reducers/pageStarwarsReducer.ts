import {InitialState, IPageStarwarsState} from "../../application/appState";
export function pageStarwarsReducer(state: IPageStarwarsState = InitialState.components.pageStarwars,
                                    action: any = null): IPageStarwarsState {
    return state;
}